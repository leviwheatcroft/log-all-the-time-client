mutation UserRegisterM(
  $name: String!
  $email: String!
  $password: String!
) {
  UserRegisterM(
    name: $name
    email: $email
    password: $password
  ) {
    accessToken
    refreshToken
  }
}
