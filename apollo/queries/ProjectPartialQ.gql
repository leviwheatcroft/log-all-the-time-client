query ProjectPartialQ(
  $projectPartial: String
  $limit: Int
  $offset: Int
) {
  ProjectPartialQ(
    projectPartial: $projectPartial
    limit: $limit
    offset: $offset
  ) {
    docs {
      ... on Project {
        id
        name
        archived
      }
    }
    hasMore
  }
}
