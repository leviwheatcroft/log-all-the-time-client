query TagPartialQ(
  $tagPartial: String
  $limit: Int
  $offset: Int
) {
  TagPartialQ(
    tagPartial: $tagPartial
    limit: $limit
    offset: $offset
  ) {
    docs {
      ... on Tag {
        id
        name
        archived
      }
    }
    hasMore
  }
}
