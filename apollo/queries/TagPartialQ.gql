query TagPartialQ(
  $tagPartial: String
  $limit: Int
  $offset: Int
  $includeArchived: Boolean
  $cacheBust: Int
) {
  TagPartialQ(
    tagPartial: $tagPartial
    limit: $limit
    offset: $offset
    includeArchived: $includeArchived
    cacheBust: $cacheBust
  ) {
    docs {
      ... on Tag {
        id
        name
        archived
      }
    }
    hasMore
  }
}
