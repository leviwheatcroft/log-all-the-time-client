const { loading } = require('./loading')
const { set } = require('./set')
const { add } = require('./add')
const { remove } = require('./remove')

module.exports = [
  loading,
  set,
  add,
  remove
]
